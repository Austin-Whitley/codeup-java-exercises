package kitchenApp;

//use the interface key word to create a new interface.
//They are always public so you dont need the "public" keyword.
//you cannot create a method inside an interface, they are always abstract

//an interface just shows how we interact or behave with objects.
public interface StoreFood {
    void keepFresh();
}
